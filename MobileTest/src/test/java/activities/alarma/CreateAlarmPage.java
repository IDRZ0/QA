package activities.alarma;

import control.Button;
import org.openqa.selenium.By;

public class CreateAlarmPage {
    public Button done = new Button(By.id("com.sec.android.app.clockpackage:id/Menu_Done"));

    public CreateAlarmPage() {
    }
}
